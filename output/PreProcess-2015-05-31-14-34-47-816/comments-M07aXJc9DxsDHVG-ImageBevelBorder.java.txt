quaqua cast graphics to graphics2d graphics2 d set some variables for easy access of insets and image size optimisation draw image directly if it fits into the component optimisation remove insets if image width or image height fits adjust insets if component is too small math max 0 left + width left right / 2 math max 0 top + height top bottom / 2 draw the leads g fillrect fill rect x+width right y x+width y+top draw the edges north south west east fill the center 