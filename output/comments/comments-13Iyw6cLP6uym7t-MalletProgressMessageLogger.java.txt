convert to subclass of logrecord log record and pass it on i'm sure this is losing information out mpml m p m l log record entered +logrecord +log record 		 getparent get parent log progressmessagelogrecord progress message log record 		 try doing the dispatch ourselves fewer classes to override whole reason for overriding is so we can not send things to console twice 		 once in progress message once in parent 		 i think this is some approximation of what logger log does 		 todo add level test and filtering 		logger 		 logger currentlogger current logger = this 		boolean senttoconsole sent to console = 		boolean useparenthandlers use parent handlers = getuseparenthandlers get use parent handlers 		while currentlogger current logger != { 			handler 			 handler handlers = currentlogger current logger gethandlers get handlers 			for i=0 i<handlers length i++ { 				if handlers i instanceof consolehandler console handler { 					if !senttoconsole !sent to console { 						handlers i publish progressmessagelogrecord progress message log record 						senttoconsole 						sent to console = 					} 				}else{ 					handlers i publish progressmessagelogrecord progress message log record 				} 			} 			if useparenthandlers use parent handlers { 				currentlogger 				current logger = currentlogger current logger getparent get parent 			} else { 				currentlogger 				current logger = 			} 		} 