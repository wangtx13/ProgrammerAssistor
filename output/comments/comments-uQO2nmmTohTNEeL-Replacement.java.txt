normally we do not allow tokens that start with numbers if the user has specifically requested a replacement that starts with a number escape it with an underscore start at the second token since we assume that the first token matches if this replacement is being matched we've matched all tokens in the source so modify the input to notify the preprocessor of the replacement output the target and advance the position 