process the command line options /*optimizemodelinterval*/ /*optimize model interval*/ start a new polylingual topic model for historical reasons we currently only support featuresequence feature sequence data not the featurevector feature vector which is the default for the input functions provide a warning to avoid classcastexceptions cast exceptions start a new lda l d a topic model /*
				 loading new data is optional if we are restoring a saved state 
				if inputfile input value != {
					instancelist {
					 instance list instances = instancelist instance list load new inputfile input value 
					system 
					 out data loaded 
					lda addinstances add instances instances 
				}
				*/ /*
			if testingfile testing value != {
				topicmodel {
				topic model settestinginstances set testing instances instancelist instance list load new testingfile testing value 
			}
			*/ 