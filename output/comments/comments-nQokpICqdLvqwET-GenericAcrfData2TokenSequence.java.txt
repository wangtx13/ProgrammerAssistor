match numeric feature supports named features offset features and features in scientific notation set the feature's value if the feature is not numeric set the value to 1 http t47962 ai mallet development aitalk info/continuous observations for dcrf t47962 html if use find use $ in regex if fixed numlabels num labels just whether we have enough otherwise use the dynamic labels separator serialization garbage 1 0 == a feature vector sequence 