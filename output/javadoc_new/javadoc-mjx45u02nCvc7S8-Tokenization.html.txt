tokenizationextends sequence getdocument get document span getspan get span i span subspan start end 所有超级接口 sequence 所有已知实现类 stringtokenization tokenization 参数 start the index of the first token in the new span inclusive this is an index of a token *not* an index into the document end the index of the first token in the new span exclusive this is an index of a token *not* an index into the document 返回 a span into this tokenization's document 