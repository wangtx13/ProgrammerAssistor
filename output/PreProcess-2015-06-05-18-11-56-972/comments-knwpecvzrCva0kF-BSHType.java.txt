cached type if available first node will either be primitivetype primitive type or ambiguousname ambiguous name note incorrect here we are using the hack in bsh manager that allows lookup by base name we need to eliminate this limitation by working through imports see notes in manager throw new interpretererror interpreter unable to resolve type +e ignore and try default out bshtype b s h type +node+ not found out found clas +clas out bshtype b s h type returning descriptor +descriptor cached type if available first node will either be primitivetype primitive type or ambiguousname ambiguous name get the type by constructing a prototype with arbitrary zero length in each dimension default zeros hack sticking to first interpreter that resolves this see comments on type instance variable is getname get name ok? test with 1 1 