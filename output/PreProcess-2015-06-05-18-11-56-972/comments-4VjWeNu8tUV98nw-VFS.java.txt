{{{ imports }}} {{{ capabilities }}} {{{ extended attributes }}} {{{ vfs v f s constructors reasonable defaults ? }}} {{{ getname get name }}} {{{ getcapabilities get capabilities }}} {{{ ismarkersfilesupported is markers supported }}} {{{ getextendedattributes get extended attributes }}} {{{ getfilename get name don't want getfilename get name roots to }}} {{{ getfilepath get path }}} {{{ getparentofpath get parent of path ignore last character of path to properly handle paths like /foo/bar/ this ensures that getfileparent get parent protocol for example is protocol and not }}} {{{ constructpath construct path }}} {{{ getfileseparator get separator }}} {{{ gettwostagesavename get two stage save name }}} {{{ reloaddirectory reload directory }}} {{{ createvfssession create v f s session }}} {{{ load this makes hypersearch hyper search much faster bufferloadrequest buffer load request can cause ui u i interations for example ftp f t p connection dialog so it should be runned in dispatch thread threadutilities thread utilities runindispatchthread run in dispatch thread request }}} {{{ save /* when doing a 'save as' the path to save to path 
		 * will not be the same as the buffer's previous path
		 * buffer getpath get path in that case we want to create
		 * a backup of the new path even if the old path was
		 * backed up as well backed_up property set */ }}} {{{ copy }}} {{{ insert }}} a name that starts with _ requires a session {{{ _canonpath }}} {{{ _listdirectory }}} {{{ _listdirectory }}} {{{ _listdirectory }}} {{{ _listfiles }}} {{{ _getfile }}} {{{ _delete }}} {{{ _rename }}} {{{ _mkdir }}} {{{ _backup to backup does not maybe the is not applicable to local filesystem but don't worry for backup purposes it may be out of a real filesystem usually that means there is no specified backup directory do copy using vfs v f s copy }}} {{{ _createinputstream }}} {{{ _createoutputstream }}} {{{ _savecomplete }}} {{{ _finishtwostagesave }}} {{{ _endvfssession }}} {{{ getdefaultcolorfor get default color for }}} {{{ directoryentrycompare directory entry compare }}} {{{ members {{{ initializer }}} {{{ recursive listfiles list files /*
			 * if looking at a symlink do not traverse the
			 * resolved path more than once 
			 */ it's a regular may be not binary }}} {{{ loadcolors load colors }}} {{{ colorentry color entry }}} {{{ sessiongetter session getter }}} }}} 