{{{ imports }}} {{{ vfsdirectoryentrytablemodel v f s directory entry table model constructor }}} {{{ setroot set root /* if files != files length != 0 
			firetablerowsdeleted 
			fire table rows deleted 0 files length 1 */ /* if files length != 0 
			firetablerowsinserted 
			fire table rows inserted 0 files length 1 */ }}} {{{ expand make a large enough destination sort expanded entries according to current sort params make room after expanded entry for subdir files /* firetablerowsinserted fire table rows inserted startindex start index + 1 
				startindex 
				start index + list size + 1 */ /* firetablerowsupdated fire table rows updated startindex start index startindex start index */ }}} {{{ collapse /* firetablerowsupdated fire table rows updated index index 
		firetablerowsdeleted 
		fire table rows deleted index + 1 lastindex last index */ }}} {{{ getcolumncount get column count }}} {{{ getrowcount get row count }}} {{{ getcolumnname get column name }}} {{{ getcolumnclass get column }}} {{{ getvalueat get value at }}} {{{ getascending get ascending }}} {{{ getsortcolumn get sort column }}} {{{ getsortattribute get sort attribute }}} {{{ sortbycolumn sort by column toggle ascending/descending if column was clicked again we don't sort by some attributes remember column }}} {{{ getextendedattribute get extended attribute }}} {{{ getcolumnwidth get column width }}} {{{ setcolumnwidth set column width }}} {{{ getfiles get files }}} {{{ members }}} {{{ members {{{ addextendedattributes add extended attributes this vfs has an extended attribute which is not in the list add it to the end with a ref count of 1 }}} {{{ removeextendedattributes remove extended attributes we no longer have any dirs using this extended attribute this vfs has an extended attribute which is not in the list ??? }}} }}} {{{ entry is this branch an expanded dir? how deeply we are nested parent entry extension }}} {{{ extendedattribute extended attribute /* reference counter allows us to remove a column from
		 * the table when no directory using this column is
		 * visible */ }}} {{{ entrycompare entry compare we want to compare sibling ancestors of the entries here we have entries of the same level here we have siblings with the same parents let's do the real comparison if the modified attribute is present then we have a localfile local sort by size sort by type = extension default sort by name }}} 