{{{ imports }}} {{{ grabkeydialog grab key dialog constructor }}} {{{ grabkeydialog grab key dialog constructor }}} {{{ getshortcut get shortcut }}} {{{ isok is o k }}} {{{ getfocustraversalkeysenabled get focus traversal keys enabled }}} {{{ processkeyevent process key event }}} {{{ members {{{ instance variables this is a bad hack }}} {{{ init create a panel with a boxlayout box layout can't use box here because box doesn't have setborder set border show remove button }}} {{{ getsymbolicname get symbolic name /* else if keycode key == keyevent key event vk_open_bracket 
			return 
		else if keycode key == keyevent key event vk_close_bracket 
			return */ }}} {{{ updateassignedto update assigned to }}} {{{ getkeybinding get key binding eg trying to bind c+n c+p if c+n already bound eg trying to bind c+e if c+e is a prefix create a temporary synthetic prefix keybinding key binding that won't be saved }}} }}} {{{ keybinding key binding }}} {{{ inputpane input pane {{{ getfocustraversalkeysenabled get focus traversal keys enabled }}} the facing is converted to a platform specific form but we keep the canonical shortcut around for internal use {{{ processkeyevent process key event }}} }}} {{{ actionhandler action handler {{{ actionperformed action performed }}} {{{ canclose can close ask whether to remove the old shortcut check whether this shortcut already check whether the other shortcut is the alt shortcut we don't need two identical shortcuts check whether shortcut is a prefix to others can't override prefix shortcuts ask whether to override that other shortcut }}} }}} 