standardutilitiesextends standard utilitiesextends text decimalformat decimal format kb_format text decimalformat decimal format mb_format charstoescapes chars to escapes str charstoescapes chars to escapes str toescape to escape getindentstring get indent str getleadingwhitespace get leading white space str getleadingwhitespace get leading white space charsequence char sequence str gettrailingwhitespace get trailing white space str getleadingwhitespacewidth get leading white space width str tabsize tab size getleadingwhitespacewidth get leading white space width charsequence char sequence str tabsize tab size createwhitespace create white space len tabsize tab size mywhitespace my whitespace = miscutilities misc utilities createwhitespace create white space mylength my length buffer getbooleanproperty get property notabs no tabs ? 0 buffer gettabsize get tab size truncatewhitespace truncate white space len tabsize tab size indentstr indent str createwhitespace create white space len tabsize tab size start mywhitespace my whitespace = miscutilities misc utilities createwhitespace create white space mylength my length buffer getbooleanproperty get property notabs no tabs ? 0 buffer gettabsize get tab size getvirtualwidth get virtual width text segment seg tabsize tab size getoffsetofvirtualcolumn get offset of virtual column text segment seg tabsize tab size column totalvirtualwidth total virtual width comparestrings compare strings str1 str2 ignorecase ignore case @deprecatedpublic @ deprecatedpublic objectsequal equal o1 o2 globtore glob to r e glob regionmatches region matches charsequence char sequence seq toff charsequence char sequence other ooff len startswith starts with charsequence char sequence seq str getboolean get obj def formatfilesize format size length byte md5 charsequence char sequence charsequence char sequence since 4 3pre5 str the since jedit j edit 4 3pre15 str the toescape to escape any characters that require escaping since jedit j edit 4 3pre15 str a the leading whitespace of that for indenting subsequent lines since jedit j edit 4 3pre10 str the str the since jedit j edit 4 3pre15 str the str the tabsize tab size the tab size str the tabsize tab size the tab size since jedit j edit 4 3pre15 len the length tabsize tab size the tab size or 0 if tabs are not to be used len the length tabsize tab size the tab size or 0 if tabs are not to be used start the start offset for tab alignment seg the segment tabsize tab size the tab size seg the segment tabsize tab size the tab size column the virtual column number totalvirtualwidth total virtual width if this is non the total virtual width will be stored in its first location if this 1 1 if the column is out of bounds str1 the first str2 the second ignorecase ignore case if case will be ignored negative if str1 lt str2 0 if both are the same positive if str1 str2 since jedit j edit 4 3pre5 since jedit j edit 4 3pre6 glob the glob pattern since jedit j edit 4 3pre7 seq the test charsequence char sequence toff offset for the test sequence other the sequence to compare to ooff offset of the comparison sequence len how many characters to compare whether the two subsequences are equal since jedit j edit 4 3pre15 see also regionmatches region matches seq the charsequence char sequence str to test whether the sequence starts with the test since jedit j edit 4 3pre15 obj the def the default value the value if obj is a if the value is yes on if the value is no off def if the value is or anything else since jedit j edit 4 3pre17 length the size since jedit j edit 4 4pre1 charsequence char sequence given md5 sum of given 