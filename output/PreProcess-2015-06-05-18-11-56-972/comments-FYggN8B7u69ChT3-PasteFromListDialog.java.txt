{{{ imports }}} {{{ pastefromlistdialog paste from list dialog constructor }}} {{{ ok }}} {{{ cancel }}} {{{ members {{{ instance variables }}} {{{ cleanup remove the reference to the jlist j list from the history model so that the list doesn't keep getting updated after the dialog is gone }}} {{{ getselectedcliptext get selected clip text these strings may be very large so if we can just the same instead of making a copy do so }}} {{{ updatebuttons update buttons }}} {{{ showcliptext show clip text }}} }}} {{{ renderer workaround for rendering labels starting with <html> using the html h t m l engine don't make the list items too large /* do nothing */ }}} {{{ actionhandler action handler }}} {{{ listhandler list handler {{{ valuechanged value changed }}} }}} {{{ mousehandler mouse handler }}} 