{{{ imports }}} {{{ getprintjob get print job for backwards compatibility the color variable is stored also as a property no need to always keep the same job name for every printout }}} {{{ pagesetup page setup }}} {{{ print }}} {{{ getpageformat get page format convert from printrequestattributeset print request attribute set to the pageformat page format doesnt exist?? pf setorientation set orientation pageformat page format reverse_portrait then just do the next best thing }}} {{{ saveprintspec save print spec for backwards compatibility the color variable is stored also as a property }}} {{{ members }}} 