begin child node structure evaluated by insurenodesparsed insure nodes parsed index of the first clause child node end child node structure evaluated by insurenodesparsed insure nodes parsed unsafe caching of type here none type t y p e or a there is always a paramsnode params node skip skip install an *instance* of this in the namespace see notes in bshmethod bsh this is not good need a way to update eval without re installing so that we can re eval params etc when classloader changes look into this validate that the names are names if strictjava strict mode check for loose and type warning callstack here don't think we need a stack trace to indicate how we sourced the warning callstack here don't think we need a stack trace to indicate how we sourced the 