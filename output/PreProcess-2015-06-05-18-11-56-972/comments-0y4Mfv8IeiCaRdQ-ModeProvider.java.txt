{{{ imports }}} {{{ removeall remove all }}} {{{ getmode get mode }}} {{{ getmodeforfile get mode for }}} {{{ getmodeforfile get mode for the check should be in reverse order so that modes from the user catalog get checked first! the very most acceptable mode is one whose name doesn't only match the name as regular expression but which is identical most acceptable is a mode that matches both the filepath and the first line glob next best is filepath match all acceptable choices are by first line glob and they all match so just the first one no matching mode found for this }}} {{{ getmodes get modes }}} {{{ addmode add mode the removal makes the insertion order in modes linkedhashmap linked hash map follow the order of addmode add mode calls }}} {{{ loadmode load mode }}} {{{ loadmode load mode }}} {{{ }}} 