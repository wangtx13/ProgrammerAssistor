splitconfigparserextends split config parserextends splitconfigparser split config parser splitconfig split config setincludesplits set include splits b setincludefiles set include files b setincluderemotefiles set include remote files b parse since jedit j edit 4 4 splitconfig split config the to parse and adjust b if retain any splits in the split configuration b if retain any names found in the split configuration b if and if include files is then retain any remote names found in the split configuration the split configuration adjusted for user preferences 