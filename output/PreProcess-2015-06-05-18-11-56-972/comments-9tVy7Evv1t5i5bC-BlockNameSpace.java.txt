/*
	note /*
	 note this essentially just delegates most of its to its
	parent the setvariable set variable indirection is very small we could probably
	fold this functionality back into the base namespace name space as a special case 
	but 
	 but this has changed a few times so i'd like to leave this abstraction for
	now 
*/ /*
		note /*
		 note it may see like with the new 1 3 scoping this test could be
		removed but it cannot when recurse is we still need to set the
		variable in our parent not here 
	*/ set the var here in the block namespace set the var in the enclosing parent namespace /*strict?*/ super variables containskey contains key name not any faster i checked begin which simply delegate to our parent enclosing scope 