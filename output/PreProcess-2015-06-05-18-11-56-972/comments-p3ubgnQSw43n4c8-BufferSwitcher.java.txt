{{{ imports }}} members item that was selected before popup menu was opened setfont set font new font dialog font bold b o l d 10 if the buffer count becomes 0 then it is guaranteed to become 1 very soon so don't do anything in that case nopmd n o p m d don't do anything if the above fails it just means dnd won't work we only strings notabs=false no tabs=false 