for operator assign operations save the lhs value before evaluating the rhs this is correct behavior for postfix operations e g i=1 i+=i++ should be 2 not 3 assign doesn't need the pre value implement blocks foo = { } if rhsnode rhs node instanceof bshblock b s h block rsh = else /*
			implement /*
			 implement += value 
			according 
			 according to the jls j l s value may be anything 
			in 
			 in beanshell bean shell we'll disallow v oid undefined values 
			 or should we map them to the empty string? 
		*/ 