{{{ imports }}} {{{ aboutdialog about dialog constructor }}} {{{ actionperformed action performed }}} {{{ closedialog close dialog }}} {{{ aboutpanel about panel pre computing all data that can be known at this time +1 makes sure every new line from below comes up smoothly cause it gets pre painted and clipped as needed draw a highlight effect check whether the text line is above the canvas if so the skips it good to go now draw only ipipelinecount i pipe line count lines and get out from loop check if the end of the list has been reached if so rewind }}} 