stringlistextends listextends arraylist list stringlist list stringlist list addall add all stringlist list split orig delim tostring to toarray to join collection c delim join arr delim join delim args all io serializable cloneable iterable collection list randomaccess random access since jedit j edit 4 3pre7 see also serialized form orig the original delim a delimiter to use for splitting a new stringlist list containing the split strings tostring to in abstractcollection collection a joined representation of this with the newline \n as delimiter specified by toarray to in collection specified by toarray to in list toarray to in arraylist list an of arr of delim delimiter to separate strings a single with each element in arr converted to a and concatenated separated by delim delim the delimiter a joined with delim inbetween each element 