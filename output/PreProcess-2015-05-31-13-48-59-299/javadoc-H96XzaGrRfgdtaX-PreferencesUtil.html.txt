prefs preferencesutil preferences preferencesutil preferences installframeprefshandler install frame prefs handler prefs preferences prefs name window window installpaletteprefshandler install palette prefs handler prefs preferences prefs name window window installpaletteprefshandler install palette prefs handler prefs preferences prefs name window window x installinternalframeprefshandler install internal frame prefs handler prefs preferences prefs name jinternalframe j internal frame window jdesktoppane j desktop pane desktop installtoolbarprefshandler install tool bar prefs handler prefs preferences prefs name jtoolbar j tool bar toolbar preferencesutil preferences installs a frame preferences handler on first run sets the window to its preferred size at the top left corner of the screen on subsequent runs sets the window the last size and location where the user had placed it before prefs preferences for storing/retrieving preferences values name base name of the preference window the window for which to track preferences installs a palette preferences handler on first run sets the palette to its preferred location at the top left corner of the screen on subsequent runs sets the palette the last location where the user had placed it before prefs preferences for storing/retrieving preferences values name base name of the preference window the window for which to track preferences installs an intenal frame preferences handler on first run sets the frame to its preferred size at the top left corner of the desktop pane on subsequent runs sets the frame the last size and location where the user had placed it before prefs preferences for storing/retrieving preferences values name base name of the preference window the window for which to track preferences installs a toolbar preferences handler on first run sets the toolbar to borderlayout border layout top t o p on subsequent runs set the toolbar to the last borderlayout border layout location prefs preferences for storing/retrieving preferences values name base name of the preference toolbar the jtoolbar j tool bar for which to track preferences 