fixme f i x m e check for the arrangement of the jinternalframes j internal frames here and the value do nothing if no frames to work with do nothing if no frames to work with do nothing if no frames to work with do nothing if no frames to work with first arrange the windows that have equal size the rest of the frames are tiled down on the last column with equal height do nothing if no frames to work with first arrange the windows that have equal size the rest of the frames are tiled down on the last column with equal height 