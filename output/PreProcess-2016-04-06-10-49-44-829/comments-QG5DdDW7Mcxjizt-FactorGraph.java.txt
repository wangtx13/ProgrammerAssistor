increases the size of all the caching arrays that need to be increased when a node is added this can also be called before he caches have been se up projectionmap projection map setdefaultvalue set default value 1 no need to recreate edgepots edge pots if it since it's a hashmap hash map dict setdefaultvalue set default value 1 no longer supported but this getindex get index written to avoid need for this do this at end b/c it affects getvertexset get vertex set xxx inefficient perhaps cache this rather than iterating over all factors just iterate over ones that we know contain c get 0 could possibly make more efficient by picking the var with smallest degree cache the factor uses brute force algorithm xxx should an assignment assumes that structure of factor graph is continous > discrete todo merge this in serialization garbage = 