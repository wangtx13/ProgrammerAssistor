here we will assume that we use all labeled data available if a features was specified then load features from the better message from dfrankow@gmail com otherwise select features using specified if the target is oracle then we do not need feature labels for other we need to get feature labels as as they haven't been already loaded from disk better message from dfrankow@gmail com todo t o d o should these be label names? 