nothing to correct rank segments by confidence if <code>findincorrect</code> <code>find incorrect</code> find the least confident segment that is incorrectly labeled else use least confident segment nothing extracted create segmentcorrectedoutput segment corrected output which has the labels for the leastconfidentsegment least confident segment and for other positions if in segment run constrained viterbi on this sequence with the constraint that this segment is tagged correctly print sequence info 