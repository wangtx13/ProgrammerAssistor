initialize jwnl j w n l this must be done before jwnl j w n l can be used running away is kind of a hard case because it involves two words that are joined by a hyphen and one of the words is not stemmed so we have to both remove the hyphen and stem running before we get to an entry that is in wordnet word net get all of the hypernyms parents of the first sense of <var>word</var> get all the hyponyms children of the first sense of <var>word</var> try to find a relationship between the first sense of <var>start</var> and the first sense of <var>end</var> find all synonyms that <var>start</var> and <var>end</var> have in common 