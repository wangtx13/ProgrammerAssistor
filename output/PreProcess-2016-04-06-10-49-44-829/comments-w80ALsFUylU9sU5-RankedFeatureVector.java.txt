/* extent of latest sort */ set the number of cells to sort making sure we don't go past the max since we are using insertion sort sorting n 1 sorts the whole reinitialize and sort selection sort swap added by limin yao rank the elements ascendingly the smaller is in the front set the number of cells to sort making sure we don't go past the max since we are using insertion sort sorting n 1 sorts the whole reinitialize and sort selection sort swap was rankorder rank order 0 xxx make this more efficient! i'm pretty sure that bitset's bit set's can do this more efficiently out rankedfeaturevector ranked feature vector getmaxvaluedindexin get max valued index in feature= +dictionary lookupobject lookup rankorder rank order i was rankorder rank order i was rankorder rank order rank was dictionary lookupobject lookup rankorder rank order rank added by limin yao 